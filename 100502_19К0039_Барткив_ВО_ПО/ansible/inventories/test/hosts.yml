---
all:
  children:
    ldap:
      hosts:
        ldap1:
          ansible_host: 10.1.1.200
          ansible_user: user

      vars:
        ansible_ssh_pass: password
        ansible_become_pass: password

    dhcp:
      hosts:
        dhcph1:
          ansible_host: 10.1.1.200
          ansible_user: user

      vars:
        ansible_ssh_pass: password
        ansible_become_pass: password

    dns:
      hosts:
        dns1:
          ansible_host: 10.1.1.200
          ansible_user: user

      vars:
        ansible_ssh_pass: password
        ansible_become_pass: password


    adminstaion:
      hosts:
        adminstation1:
          ansible_host: 10.1.1.200
          ansible_user: user

      vars:
        ansible_ssh_pass: password
        ansible_become_pass: password

